version: "3.8"

services:
  kajukenbo-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kajukenbo-website
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
    restart: unless-stopped

